events {}

http {
  include mime.types;

  server {
    listen 80;

    location /api/ {
      proxy_pass http://server:5000/;
    }

    location / {
      root /client-build;
      try_files $uri /index.html;
    }
  }
}